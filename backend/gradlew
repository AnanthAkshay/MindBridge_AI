#!/bin/sh
#
# Copyright © 2015-2021 the original authors.
# Licensed under Apache 2.0 — see https://www.apache.org/licenses/LICENSE-2.0
#
##############################################################################
# Gradle wrapper start-up script for POSIX-compatible shells.
##############################################################################

# Attempt to set APP_HOME
APP_HOME="$(cd "$(dirname "$0")" && pwd -P)"

APP_NAME="Gradle"
APP_BASE_NAME="$(basename "$0")"

DEFAULT_JVM_OPTS='"-Xmx64m" "-Xms64m"'

# Use the maximum available physical memory on the JVM or the default (256 MB)
MAX_FD="maximum"

CLASSPATH="$APP_HOME/gradle/wrapper/gradle-wrapper.jar"

JAVACMD="${JAVA_HOME}/bin/java"
[ -z "$JAVA_HOME" ] && JAVACMD="java"

exec $JAVACMD \
  $DEFAULT_JVM_OPTS \
  $JAVA_OPTS \
  $GRADLE_OPTS \
  "-Dorg.gradle.appname=$APP_BASE_NAME" \
  -classpath "$CLASSPATH" \
  org.gradle.wrapper.GradleWrapperMain \
  "$@"
